# STEP 25 — User visual QA corrections and regression protocol
Date: 2026-09-16. Authority: user-supplied approved 1536×1024 home reference and subsequent user screenshots of financing, news and industry cards. This document supplements `STEP25_APPROVED_HOME_HEADER_DESIGN_LOCK_2026-09-16.md`; do not replace its approved composition.

## ACCEPTED / LOCKED
- User explicitly ACCEPTED the white IIG logo with vertical divider and `INDUSTRY INTELLIGENCE GENERATION` wordmark. Do not alter logo, recolor to navy, reintroduce `Industrial Energy Intelligence Platform`, or replace it with text-only fallback as a design choice. Existing `assets/iig-logo-white.svg` is the approved asset after tagline removal (commit `8e388fca1d6a6b93445f10c50cd0c2d3a0f3965d`).
- Digest must be a real PDF download over the lower-right of the hero, not a link to HTML. The large 360px card was rejected; compact 245px width and reduced type/icon were implemented in shared CSS/JS. Keep compact; verify actual visual dimensions and that PDF exists/downloads.
- Shared top navigation: navy #06223d, white logo and links, yellow active page and language; same layout across ALL sections. Header/hero distinction: header means navigation bar; page hero/banner below it must ALSO use the same approved original sunrise industrial panorama on every page, never the old pipe photo. The user's news screenshot shows BOTH a white legacy nav and pipes: visual regression; do not infer fix merely from a shared CSS file being present.
- Exact approved panorama file in repository root: `Вариант шапки сайта_промкомплекса на рассвете ( розово-фиалетовый).png`; engineer: `Принятое фото главного инженера в каске IIG.png`. No screenshot crops, Unsplash pipes, or unapproved WEBP stand-ins.

## Corrections recorded from user QA
1. NEWS / ALL INTERNAL PAGES: `news.html` screenshot still showed legacy white navigation and pipe photograph. Apply common approved header and panorama to news, financing, advice, about, industry, finance-news and any other public page; inspect actual markup, CSS specificity/load order, page-specific inline styles and script loading. Fix the ROOT cause rather than piling up untested `!important` overrides. Verify each published URL with screenshots and asset requests. A change to `.page-hero` alone does not prove the header or banner is fixed.
2. INDUSTRY CARD TEXT: screenshot showed words clipped horizontally in narrow numbered industry cards. Allow normal wrapping and breaking of long words, avoid fixed narrow widths / overflow clipping, adjust font size and line height to card width and responsive breakpoint, preserve readable UA and EN titles, inspect all nine cards on desktop and mobile. Commit `6dc6c13064f11f556f68035813d43ccfeba1d119` was reported as a CSS fix, but published screenshot verification remains required.
3. UA/EN GLOBAL: user found language switch does not work on every section. Current `assets/iig.js` only translates elements carrying `data-ua`, `data-en`, `data-ua-html` or placeholder attributes; untranslated hardcoded content remains on internal pages. Fix ALL user-facing content in each section, not just menu. Persist selection across navigation/reload; check UA→EN→UA on home, financing, news, advice, about, industry, finance-news, forms and relevant routes; check headings, breadcrumbs, cards, filters, CTAs, aria labels, placeholders, dynamic content and active language. Never label language QA GREEN until verified; as of this protocol update this is OPEN / BLOCKING.
4. ACCEPTANCE: screenshots are user evidence, not automatically a pass/fail of subsequent deployments. After each change check exact HEAD commit, Actions success, live Pages response and side-by-side screenshot at approved 1536×1024 plus 1440/1920/mobile. Verify no 404, missing logo, clipped cards, stale hero, or language regressions. Record concrete evidence and unresolved issues; CI success = DEPLOY GREEN only, NOT visual or overall STEP 25 GREEN.

## Implementation status / traceability
- Earlier image/hero/digest implementation commits: `df0492ed`, `bdf94da`; protocol update `764fc48`.
- White logo and compact digest/shared CSS work: `8e388fca` and preceding shared stylesheet changes. Logo accepted by user.
- Industry/hero adjustment: `6dc6c13064f11f556f68035813d43ccfeba1d119` reported committed; requires independent live visual confirmation.
- UA/EN content coverage: NOT COMPLETE; fix pending.
- Cross-page news header and sunrise banner: user-reported regression; code-level intent is not proof of published result; live verification pending.

## Mandatory no-repeat gate
Before saying `DONE`, `GREEN`, or `fixed`: read both STEP25 protocols, inspect all page templates and shared CSS/JS, implement missing changes, create commit, verify successful deployment for exact commit, visually compare published site with user reference on all relevant pages and breakpoints, test bilingual switch end-to-end, then state pass/fail per item. If any gate fails, explicitly report pending; never imply a commit or green CI is visual approval.
